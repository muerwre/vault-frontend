// @vault/common — shared enums, wire DTOs and validation (the client/server contract).
// Populated in Phase 1 from examples/migration-spec/data-model.md.
export const VAULT_COMMON_PACKAGE = '@vault/common';
